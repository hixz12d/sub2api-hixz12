# 已知问题记录

> 记录日期：2026-08-14  
> 当前分支：`fix/openai-egress-fail-closed`  
> 当前提交：`6ea8edeff149c134a50537398df538b47608117a`  
> 修改前基线：`079f9aafb`  
> 环境：Windows/amd64、Go 1.26.5、`CGO_ENABLED=0`

## 1. 结论摘要

运行后端全仓测试：

```bash
cd backend
go test ./...
```

结果有 **2 个 package 失败、共 23 个测试失败**：

- `github.com/Wei-Shaw/sub2api/internal/service`：22 个失败；
- `github.com/Wei-Shaw/sub2api/cmd/openai-wsv2-canary`：1 个失败。

这些失败不能简单归因于本次 OpenAI/Codex 出口加固：已经从修改前提交 `079f9aafb` 创建隔离 worktree，并复现了两个具有代表性的失败：

- `TestOpenAIGatewayService_SelectAccountWithScheduler_SubscriptionPriorityChoosesSubscriptionPoolFirst`：`sticky session not found`；
- `TestOpenAIGatewayServiceForwardOAuthRemoteCompactV2PreservesResponsesWire`：请求头 `x-codex-beta-features` 预期为 `remote_compaction_v2`，实际为空。

因此至少调度类和 Codex metadata 类失败在出口加固修改前就已存在。为了避免扩大本次安全加固范围，没有顺手修改这些无关实现或测试。其余失败尚未逐个在基线 worktree 中独立复现，修复时仍应分别验证，不能仅凭本记录认定全部都是基线问题。

## 2. 当前提交关系

当前加固提交建立在以下 3 个尚未位于 `origin/main` 的本地功能提交之上：

```text
6ea8edeff fix(openai): enforce fail-closed proxy egress
079f9aafb feat(openai): persist durable affinity ownership
6b509cc2b feat(openai): unify retry budget and stream replay bounds
7535478ea refactor(openai): finalize Codex identity snapshots
63cc910b4 fix(openai): complete Codex shared identity convergence (origin/main)
```

排查下面的问题时，建议优先比较 `63cc910b4..079f9aafb`，尤其关注 identity snapshot、retry/replay 和 durable affinity 对测试夹具及默认行为的影响。

## 3. 失败分类和详细清单

### 3.1 OpenAI 调度与 sticky session：9 个

共同现象：测试调用账号调度器时收到：

```text
Received unexpected error:
    sticky session not found
```

失败测试：

1. `TestOpenAIGatewayService_BindingPriorityBypassesLowerTierStickyAfterRecovery`
2. `TestOpenAIGatewayService_BindingPriorityExhaustsPrimaryPeersBeforeFallback`
3. `TestOpenAIGatewayService_BindingPriorityModeUsesCurrentGroupTier`
4. `TestOpenAIGatewayService_SelectAccountWithScheduler_LoadBalanceDistributesAcrossSessions`
5. `TestOpenAIGatewayService_SelectAccountWithScheduler_SubscriptionPriorityChoosesSubscriptionPoolFirst`
6. `TestOpenAIGatewayService_SelectAccountWithScheduler_SubscriptionPriorityDisabledUsesScore`
7. `TestOpenAIGatewayService_SelectAccountWithScheduler_SubscriptionPriorityFallsBackWhenSubscriptionFull`
8. `TestOpenAIGatewayService_SelectAccountWithScheduler_SubscriptionPriorityWaitsOnBusySubscriptionWhenRegularUnusable`
9. `TestOpenAIGatewayService_SelectAccountWithScheduler_UsesAccountPriorityWithinGroupPool`

主要测试文件：

- `backend/internal/service/openai_account_scheduler_test.go`

已知代表位置：

- `openai_account_scheduler_test.go:2615`
- `openai_account_scheduler_test.go:2878`
- `openai_account_scheduler_test.go:3596`

建议排查：

1. 检查 `079f9aafb` 的 durable affinity ownership 改动是否让“找不到 sticky session”从可回退条件变成硬错误；
2. 检查这些测试构造的 `schedulerTestGatewayCache` 是否缺少新的 affinity/sticky 数据；
3. 明确期望语义：首次出现的新 `sessionHash` 应进入普通选号，还是必须已有 sticky 记录；
4. 如果实现语义正确，应更新测试夹具；如果新 session 本应允许普通选号，则修正调度器的错误传播/回退分支；
5. 修复后分别运行上面 9 个测试，并至少追加一次 `-count=20` 检查稳定性。

### 3.2 Codex beta/turn metadata 丢失：4 个

三个测试的共同现象：

```text
expected: "remote_compaction_v2"
actual  : ""
```

失败测试：

1. `TestOpenAIGatewayServiceForwardOAuthRemoteCompactV2PreservesResponsesWire`
2. `TestOpenAIGatewayService_Forward_WSv2_OAuthStoreFalseByDefault`
3. `TestOpenAIGatewayService_OAuthPassthrough_StreamKeepsToolNameAndBodyNormalized`

另一个 WebSocket 测试的现象：

```text
expected: "turn-meta-1"
actual  : ""
```

4. `TestOpenAIGatewayService_ProxyResponsesWebSocketFromClient_PassthroughHeadersUsePromptCacheAndTurnState`

主要测试文件：

- `backend/internal/service/openai_gpt56_max_test.go`
- `backend/internal/service/openai_oauth_passthrough_test.go`
- `backend/internal/service/openai_ws_forwarder_success_test.go`
- `backend/internal/service/openai_ws_forwarder_ingress_session_test.go`

建议排查：

1. 检查 Codex identity snapshot 重构后，请求头是否从 Gin context、原始请求或 snapshot 中丢失；
2. 检查 HTTP passthrough、WS 首帧和 WS 后续 turn 是否使用同一套 metadata 合并规则；
3. 检查 `x-codex-beta-features` 的规范化或 allowlist 是否错误删除 `remote_compaction_v2`；
4. 检查 turn state 重置是否过早清除了 `turn-meta-1`；
5. 同时验证 HTTP、SSE、WebSocket，避免只修某一个传输路径。

### 3.3 Claude Code todo guidance 兼容测试：6 个

失败测试：

1. `TestForwardAsAnthropic_APIKeyMetadataSessionSurvivesChangingCacheControlAnchorAfterContinuationDisabled`
2. `TestForwardAsAnthropic_AttachesPreviousResponseIDForCompatContinuation`
3. `TestForwardAsAnthropic_OAuthAddsClaudeCodeTodoGuardForCompatModel`
4. `TestForwardAsAnthropic_OAuthCompatKeepsFullReplayForCacheGrowth`
5. `TestForwardAsAnthropic_ReplaysWithoutContinuationWhenPreviousResponseMissing`
6. `TestForwardAsAnthropic_TrimsFullReplayOnlyForCodexCompatModels`

主要测试文件：

- `backend/internal/service/openai_compat_model_test.go`

典型断言：

```go
require.Contains(
    t,
    gjson.GetBytes(body, "input.0.content.0.text").String(),
    "<claude-code-todo-guidance>",
)
```

典型错误非常反常：错误输出显示被检查文本表面上已经以 `<claude-code-todo-guidance>` 开头，但 `require.Contains` 仍报告不包含该字符串。单独运行其中一个测试也能稳定复现，并非只在全仓并行测试中出现。

建议排查：

1. 在失败断言前临时打印 `%q`、字符串长度及 `[]byte` 十六进制，确认尖括号、连字符或 Unicode 是否含不可见字符；
2. 将 `gjson.GetBytes(...).String()` 保存到局部变量，分别检查 `strings.Contains` 与 `require.Contains`；
3. 检查依赖版本及是否存在自定义/替换后的 testify 行为；
4. 检查 JSON 序列化后是否存在转义、重复 guidance、错误 content item 或路径命中了非预期字段；
5. 不要仅为了让测试变绿而删除 guidance；先确认生产请求应保留的真实 wire 格式。

单测复现命令：

```bash
cd backend
go test ./internal/service \
  -run '^TestForwardAsAnthropic_TrimsFullReplayOnlyForCodexCompatModels$' \
  -count=1
```

### 3.4 OAuth enrichment 请求次数：1 个

失败测试：

- `TestOpenAIOAuthServiceEnrichmentReusesSnapshotIdentity`

位置：

- `backend/internal/service/openai_oauth_service_refresh_test.go:106`

现象：

```text
expected request count: 2
actual request count  : 1
```

捕获到的唯一请求仍带有预期 identity headers，例如 `User-Agent`、`Originator` 和 `Version`。需要确认第二个 enrichment 请求为何不再发生。

建议排查：

1. 明确两个预期请求分别对应 profile、subscription、privacy 或其他哪个 endpoint；
2. 检查 `SubscriptionExpiresAt` 已存在时是否新增了跳过请求的优化；
3. 检查 snapshot identity 重构是否将两个调用合并或短路了其中一个；
4. 若当前实现只发一个请求是有意优化，应修改测试为验证“所有实际发出的 enrichment 请求都复用同一 identity”，而不是固定断言数量为 2；
5. 若两个请求仍是业务必需，则恢复遗漏调用并验证二者都通过已解析的出口路由。

单测复现命令：

```bash
cd backend
go test ./internal/service \
  -run '^TestOpenAIOAuthServiceEnrichmentReusesSnapshotIdentity$' \
  -count=1
```

### 3.5 运行时快照 stale-cache 超时：2 个

失败测试：

1. `TestContentModerationRuntimeSnapshotRefreshFailureKeepsStaleConfig`
2. `TestRecordCyberPolicyEvent_RuntimeSnapshotRefreshFailureKeepsStaleScope`

现象：

```text
Condition never satisfied
```

已知位置：

- `backend/internal/service/content_moderation_runtime_cache_test.go:279`

测试在等待快照刷新失败后继续保留旧配置/旧 scope，但预期条件在等待窗口内没有出现。

建议排查：

1. 检查 TTL、刷新间隔和测试等待超时是否依赖真实时钟；
2. 检查后台 goroutine 是否启动、是否被提前停止、错误是否被吞掉；
3. 检查 Go 1.26 或 Windows 定时器调度下是否暴露时间边界问题；
4. 优先改为可注入 clock/ticker 的确定性测试，不要单纯无限增加 sleep；
5. 使用 `-count=20` 评估是否为稳定失败还是时序偶发失败。

### 3.6 Windows canary Key 文件权限：1 个

失败测试：

- `TestRunCompletesAndReportsCorrelation`

package：

- `github.com/Wei-Shaw/sub2api/cmd/openai-wsv2-canary`

位置：

- `backend/cmd/openai-wsv2-canary/main_test.go:54`

现象：

```text
key file permissions must not exceed 0600 (got 0666)
```

该测试单独运行也稳定失败。Windows 的文件权限语义与 Unix mode bits 不完全等价，临时文件可能显示为 `0666`，而实现强制要求不超过 `0600`。

建议排查：

1. 明确生产要求：Windows 上应验证 ACL，还是只在 Unix 平台检查 mode bits；
2. 测试创建 key 文件后可显式 `os.Chmod(path, 0600)`，但要确认 Windows 上该调用能满足实现检查；
3. 如果实现本就只支持 Unix 权限模型，应给测试增加平台条件，并为 Windows 添加 ACL/替代行为测试；
4. 不建议直接取消私钥权限检查。

单测复现命令：

```bash
cd backend
go test ./cmd/openai-wsv2-canary \
  -run '^TestRunCompletesAndReportsCorrelation$' \
  -count=1
```

## 4. 本次 OpenAI 出口加固已通过的检查

以下检查已经通过：

```bash
cd backend

go test ./internal/service \
  -run 'TestOpenAIEgress|TestOpenAIWSCompatibilityIncludesRouteKey|TestUpdateAccountInvalidatesProbeSnapshotWhenProxyChanges' \
  -count=1

go test ./internal/config ./internal/pkg/httpclient ./internal/pkg/proxyurl

go test ./cmd/server -run '^$'
go test ./internal/server -run '^$'

go vet ./internal/service ./internal/handler ./internal/config
```

覆盖内容包括：

- `proxy_required` 下缺少代理时 fail closed；
- optional 模式允许显式 direct route；
- IPv6 SOCKS5 代理规范化为 `socks5h`；
- 代理查找失败不回落直连且不泄漏后端错误；
- 禁用、过期和 direct-fallback 代理被拒绝；
- `RouteKey` 不包含明文凭据，且代理凭据/版本变化会改变 route identity；
- WebSocket 连接和预热按 `RouteKey` 隔离；
- 代理变更触发账号连接状态失效；
- server 依赖注入代码可构建。

## 5. Race 检查限制

尝试执行：

```bash
cd backend
go test -race ./internal/service \
  -run 'TestOpenAIEgress|TestOpenAIWSCompatibilityIncludesRouteKey' \
  -count=1
```

默认失败原因：

```text
go: -race requires cgo; enable cgo by setting CGO_ENABLED=1
```

启用 `CGO_ENABLED=1` 后失败原因：

```text
cgo: C compiler "gcc" not found
```

因此当前不能声称 race test 已通过。后续应在安装了受支持 C 编译器的 Windows 环境，或在 Linux CI 中补跑：

```bash
CGO_ENABLED=1 go test -race ./internal/service \
  -run 'TestOpenAIEgress|TestOpenAIWSCompatibilityIncludesRouteKey' \
  -count=1
```

## 6. 建议修复顺序

1. **先修 scheduler/sticky session**：失败数量最多，可能影响真实账号选择和 failover；
2. **再修 Codex metadata/turn state**：关系到 HTTP、passthrough 和 WebSocket 请求一致性；
3. **确认 OAuth enrichment 请求语义**：避免误删必要的订阅或隐私请求；
4. **排查 Claude guidance 的字节级异常**：先确认 wire 内容，再决定改实现还是测试；
5. **修复两个 runtime snapshot 时序测试**：优先引入可控 clock；
6. **处理 Windows canary 权限测试**：保留私钥安全检查，做平台适配；
7. 每修完一组先跑该组的定向测试，最后再运行 `go test ./...` 和可用环境下的 `go test -race`。

## 7. 验收标准

完成修复后至少满足：

- `go test ./...` 无失败；
- 调度测试在 `-count=20` 下稳定；
- HTTP、SSE、passthrough、WebSocket 的 Codex metadata 测试全部通过；
- runtime snapshot 测试不依赖脆弱的真实 sleep；
- Windows 和 Linux 的 canary key 权限行为都有明确测试；
- 安装 C 编译器的 CI/开发环境中，相关包 `go test -race` 通过；
- 不削弱 OpenAI/Codex `proxy_required` 的 fail-closed、`same_route` failover 和 WebSocket `RouteKey` 隔离。
